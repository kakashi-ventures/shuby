Show current Shuby development progress at a glance.

## Instructions

1. Read `docs/PROGRESS.md` and display the feature completion checklist
2. Run `git log --oneline -10` to show recent activity
3. If `.claude/HANDOFF.md` exists, read and display the last session's context
4. Summarize:
   - How many v1.0 features are complete vs remaining
   - What's currently in progress (from HANDOFF.md)
   - Any blockers noted
5. Run `git status --short` to show uncommitted changes
6. Present a clear, concise status report

<% unless user_signed_in? %>
  <%= nav_link_to t(".pricing"), pricing_path if Jumpstart.config.payments_enabled? %>
<% else %>
  <%= render "account_menu" if Jumpstart.config.team_accounts? && Current.other_accounts.any? %>
<% end %>

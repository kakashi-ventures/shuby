<%# Activity hero: short blue band (Figma 532:24578, FA 6.2.2 "blue for activities").
    No cover image — just a tinted strip behind the floating back button before
    the white panel slides up over it. Back button lives in the page-level
    floating wrapper in archive/show.html.erb so it persists during scroll —
    don't render it here. The pt-16 leaves headroom under the floating button. %>
<div class="relative bg-shuby-blue-400 pt-16 pb-10"></div>

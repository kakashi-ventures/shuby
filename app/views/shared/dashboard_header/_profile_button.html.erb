<%# Child profile button with avatar %>
<%= link_to (current_child ? child_path(current_child) : children_path),
            class: "w-10 h-10 rounded-full bg-white flex items-center justify-center overflow-hidden border-2 border-white shadow-sm" do %>
  <% if current_child&.avatar&.attached? %>
    <%= image_tag current_child.avatar, class: "w-full h-full object-cover" %>
  <% elsif current_child %>
    <span class="shuby-p1-dark text-[var(--color-shuby-blue-800)]">
      <%= current_child.display_name.first.upcase %>
    </span>
  <% else %>
    <%= render_svg "shuby/icons/icon-account", size: :md, styles: "text-gray-400", decorative: true %>
  <% end %>
<% end %>

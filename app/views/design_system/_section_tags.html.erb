<!-- Tags Section (with Icon) -->
<section class="mb-12">
  <h2 class="shuby-h3 mb-6">Tags (with Icon)</h2>

  <%= render "design_system/tags_variants" %>
  <%= render "design_system/tags_details" %>
</section>

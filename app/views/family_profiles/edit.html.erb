<%= native_form_tag %>
<% Current.meta_tags.set(title: t(".title")) %>

<div class="min-h-screen bg-gray-50 dark:bg-gray-900">
  <div class="max-w-lg mx-auto px-4 py-6 pb-24">
    <div class="mb-6">
      <%= render "shared/ui/back_link", path: root_path, label: t("common.back"), class: "mb-4" %>
      <h1 class="shuby-h2 text-gray-900 dark:text-white"><%= t(".title") %></h1>
      <p class="shuby-p1 text-gray-500 mt-1"><%= t(".subtitle") %></p>
    </div>

    <div class="mb-6 p-4 bg-white dark:bg-gray-800 rounded-2xl border border-gray-200 dark:border-gray-700">
      <%= render "shared/ui/progress_bar",
          percentage: @family_profile.profile_completeness_percentage,
          label: t(".completeness"),
          show_percentage: true %>
    </div>

    <%= form_with model: @family_profile, url: family_profile_path, method: :patch, class: "space-y-6" do |form| %>
      <%= render "error_messages", resource: @family_profile %>
      <%= render "family_profiles/section_family_info", form: form %>
      <%= render "family_profiles/section_family_structure", form: form %>
      <%= render "family_profiles/section_caregivers", family_profile: @family_profile %>
      <%= render "family_profiles/section_hereditary", form: form, family_profile: @family_profile %>
      <%= render "family_profiles/section_children_list", children: @children %>

      <div class="pt-2">
        <%= form.submit t(".save"), class: "shuby-btn shuby-btn-lg shuby-btn-primary w-full" %>
      </div>
    <% end %>
  </div>
</div>

<% content_for :bottom_nav do %>
  <%= render "shared/shuby_bottom_nav", active: "profilo" %>
<% end %>

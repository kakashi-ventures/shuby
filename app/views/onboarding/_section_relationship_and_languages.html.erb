<div>
  <span class="shuby-form-legend"><%= t("onboarding.relationship") %></span>
  <div class="grid grid-cols-2 gap-2">
    <% [:dad, :mom, :grandparent, :caregiver, :other].each do |rel| %>
      <label class="relative cursor-pointer">
        <input type="radio" name="account_user[relationship_to_child]" value="<%= rel %>"
               class="peer sr-only"
               <%= "checked" if account_user.relationship_to_child == rel.to_s %>>
        <span class="shuby-radio-pill">
          <%= t("onboarding.relationship_options.#{rel}") %>
        </span>
      </label>
    <% end %>
  </div>
</div>

<div>
  <span class="shuby-form-legend"><%= t("onboarding.languages") %></span>
  <div class="grid grid-cols-3 gap-2">
    <% [1, 2, 3].each do |count| %>
      <label class="relative cursor-pointer">
        <input type="radio" name="family_profile[languages_spoken_at_home]" value="<%= count %>"
               class="peer sr-only"
               <%= "checked" if family_profile.languages_spoken_at_home == count %>>
        <span class="shuby-radio-pill">
          <%= count == 3 ? "3+" : count %>
        </span>
      </label>
    <% end %>
  </div>
  <p class="shuby-p2 text-shuby-blue-600 mt-2 text-center">
    <%= t("onboarding.languages_help") %>
  </p>
</div>
